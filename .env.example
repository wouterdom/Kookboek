# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

# Public keys (safe for client-side)
NEXT_PUBLIC_SUPABASE_URL=http://your-server-ip:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Private keys (server-side only - NEVER expose to client!)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Database connection (for migrations and admin tasks)
DATABASE_URL=postgresql://postgres:your_database_password@your-server-ip:5432/postgres

# JWT Secret (for token verification)
JWT_SECRET=your_jwt_secret_here

# Server Information (for reference)
SERVER_IP=your-server-ip
SERVER_USERNAME=your-username
